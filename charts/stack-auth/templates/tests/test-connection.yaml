apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stack-auth.fullname" . }}-test-connection"
  labels:
    {{- include "stack-auth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stack-auth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

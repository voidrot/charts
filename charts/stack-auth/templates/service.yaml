apiVersion: v1
kind: Service
metadata:
  name: {{ include "stack-auth.fullname" . }}
  labels:
    {{- include "stack-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stack-auth.selectorLabels" . | nindent 4 }}
